name: Ceipal Ticker (Disabled - Migrated to Vercel)

# This workflow has been disabled.
# The project is now deployed on Vercel which handles:
# - Auto-deployment on every push to main
# - Secure server-side environment variables
# - Fresh Ceipal token generation via /api/apply route
# - Gemini API key via /api/analyze route

on:
  workflow_dispatch: # Only runs if manually triggered
